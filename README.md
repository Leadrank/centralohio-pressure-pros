# Central Ohio Pressure Pros

Generated by **site-factory** from `specs/centralohio-pressure-pros.spec.mjs`.
Astro + Cloudflare. To change content, edit the spec and re-run the generator — do
not hand-edit `src/consts.ts` or `src/content.ts` (they are overwritten).

```
npm install
npm run build      # -> dist/
npm run dev        # local preview
npm run deploy     # build + wrangler deploy
```
