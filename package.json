{
  "name": "centralohio-pressure-pros",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "deploy": "npm run build && wrangler deploy && npm run indexnow && npm run gsc-index",
    "indexnow": "node scripts/indexnow-submit.mjs",
    "cf-typegen": "wrangler types",
    "gsc-index": "node scripts/gsc-index.mjs"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^5.1.1"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
